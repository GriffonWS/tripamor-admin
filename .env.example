# Same MongoDB URI you use for tripamor_backend
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/tripamor

# Random secret used to sign admin session cookies
# Generate one with: openssl rand -base64 32
AUTH_SECRET=replace-me-with-a-long-random-string

# Plain-text password for the single admin user
ADMIN_PASSWORD=change-me